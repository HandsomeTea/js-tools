import * as date from '../src/date';

describe('date', () => {
    test('getTimeDuration', () => {

    });

    test('getUTCTime', () => {

    });

    test('getDateInfo', () => {

    });

    test('getDateStart', () => {

    });

    test('formatTimeLong', () => {
        expect(date.formatTimeLong(812000)).toBe('00:13:32');
    });
});
