import * as array from '../src/array';

describe('array', () => {
    test('oneOfArray', () => {
        expect(array.oneOfArray([123, '22'])).toBeDefined();
    });

    test('arrayToObject', () => {

    });
});
